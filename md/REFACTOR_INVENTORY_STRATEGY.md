# 重構藍圖：庫存策略分倉管理

本文件追蹤資料庫重構專案的進度，目標是實現基於分倉的獨立庫存策略。

## 總體目標

重構目前全域性的庫存策略，實現讓**單一零件在不同倉庫地點（Location）可以擁有獨立的「補貨點」與「安全庫存」**。

---

## 開發與驗證計畫

- [x] **建立藍圖檔案**

### 階段一：資料庫重構

- [x] **1.1. 修改模型**:
    - [x] 從 `Part` 模型中移除 `reorder_point` 和 `safety_stock`。
    - [x] 在 `CurrentInventory` 模型中新增 `reorder_point` 和 `safety_stock`。
- [x] **1.2. 產生遷移腳本**:
    - [x] 執行 `flask db migrate` 產生腳本。
- [x] **1.3. 編輯遷移腳本**:
    - [x] 手動編輯腳本，加入資料轉移邏輯，確保現有數據平滑過渡。
- [x] **1.4. 應用遷移**:
    - [x] 執行 `flask db upgrade` 套用變更。
    - [x] **(驗證)** 抽樣檢查資料庫，確認資料已成功轉移。

### 階段二：後端邏輯重構

- [x] **2.1. 識別相依程式碼**:
    - [x] 搜尋專案中所有 `reorder_point` 和 `safety_stock` 的使用之處。
- [x] **2.2. 重構業務邏輯**:
    - [x] 修改後端服務與控制器，將邏輯切換至 `CurrentInventory`。
- [ ] **2.3. 後端驗證**:
    - [ ] **(驗證)** 執行既有測試或手動測試庫存相關 API，確保邏輯正確。

### 階段三：前端介面重構

- [x] **3.1. 重構前端模板與腳本**:
    - [x] 修改 `inventory` 相關頁面的模板與 JavaScript。
- [ ] **3.2. 前端驗證**:
    - [ ] **(驗證)** 在瀏覽器中實際操作，確認頁面顯示與編輯功能符合新規格。

---

## Git 提交紀錄

*此處將記錄相關的 Commit Hash。*
